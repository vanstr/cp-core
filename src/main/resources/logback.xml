<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- !!!Keep in mind  -->
    <!-- when cp-core app is deployed on Jboss server, this file settings is ignored -->
    <!-- for that, additional settings should be added to Jboss -->

    <!-- TODO: change properties file -->
    <property resource="log.properties" />

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>%d{HH:mm:ss} %-5level %logger{36} - %msg%n
            </Pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${PROJECT_HOME_DIR}/${LOGGING_FILE}</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>%d{yyyy-MM-dd_HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
            </Pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
            <FileNamePattern>${PROJECT_HOME_DIR}/${LOGGING_FILE}.%i.zip</FileNamePattern>
            <MinIndex>1</MinIndex>
            <MaxIndex>10</MaxIndex>
        </rollingPolicy>

        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <MaxFileSize>20MB</MaxFileSize>
        </triggeringPolicy>

    </appender>


    <!-- EXAMPLES
    applied to specified class
    <logger name="Run" level="info" />

    applied to all classes in "cloud" package
    <logger name="cloud" level="debug" />
     -->

    <root level="debug">
        <appender-ref ref="FILE" />
        <appender-ref ref="STDOUT" />
    </root>

</configuration>